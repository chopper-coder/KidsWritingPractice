# GitHub Pages 部署

1. 將本資料夾內所有檔案上傳至 Repository 根目錄。
2. Settings → Pages。
3. Source 選 `Deploy from a branch`。
4. Branch 選 `main`，Folder 選 `/ (root)`。
5. 儲存後等待 Pages 部署完成。

> 自行上傳的水果照片不會出現在 GitHub Repository；照片保存在每位使用者自己的瀏覽器 IndexedDB。要搬到其他裝置，請使用 V2.4.4「ZIP 完整家庭備份」匯出，再到新裝置匯入。
