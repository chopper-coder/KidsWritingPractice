# V2.4.4 資安檢查

## ZIP 匯入防護

- 只接受本程式 V2.4.4 產生的 STORE 模式 ZIP。
- 拒絕加密 ZIP、非 STORE 壓縮方法、重複檔名與不安全路徑。
- 防止 `../` 路徑穿越與絕對路徑。
- 驗證每個 ZIP entry 的 CRC32。
- ZIP 檔案數上限 2500，總資料上限 120 MB。
- 圖片僅接受 PNG、JPEG、WebP，並再次套用既有圖片大小限制。
- 自訂水果 `photoId` 必須能找到對應照片，否則完整備份拒絕還原。

## 隱私

所有 ZIP 建立、解析與 IndexedDB 還原均在瀏覽器本機完成，不會自動上傳到 GitHub 或第三方伺服器。
